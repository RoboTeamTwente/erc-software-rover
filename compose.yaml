services:
  ros-desktop:
    image: osrf/ros:kilted-desktop-full
    command: [gz, sim, shapes.sdf]
    environment:
      DISPLAY: ${DISPLAY}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
    devices:
      - /dev/dri
